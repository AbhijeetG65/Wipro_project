#include<stdio.h>

int main(){
    
    int a, b, sum, divide, mult, mod;
    scanf("enter value of a and b %d%d",&a,&b);

    sum = a + b;
    printf("sum of a and b is: %d",sum);

    mult = a*b;
    printf("division of a and b is: %d", mult);

    mod = a%b;
    printf("division of a and b is: %d", mod);

}